Initialize
   git clone git@github.com:prettybigmonster/VIM_Config.git    ~/.vim

Create SymLinks
   ln -a ~/.vim/.vimrc ~/.vimrc

Switch to ~/.vim directory and fetch subomain
   cd ~/.vim
   git submodule init
   git submodule update
